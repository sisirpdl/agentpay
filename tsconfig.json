{
  "compilerOptions": {
    // --- 1. The Critical Fixes ---
    "module": "commonjs",           // Standard Node.js behavior (fixes import errors)
    "moduleResolution": "node",     // Finds files like 'src/db' correctly
    "target": "es2022",
    "lib": ["es2022", "dom"],
    "types": ["node"],              // Fixes "Cannot find name 'process'"

    // --- 2. File Locations ---
    "rootDir": "./",                // Allows files in root (like test-db.ts) and src/ to be compiled
    "outDir": "./dist",             // Keeps your project clean
    
    // --- 3. Compatibility ---
    "esModuleInterop": true,        // Allows importing CommonJS modules easily
    "skipLibCheck": true,           // Ignores errors in library files (saves sanity)
    "strict": true,                 // Good practice, keeps code safe

    // --- 4. Disable the settings that caused your specific errors ---
    "verbatimModuleSyntax": false,  // This was causing the "import/export" error
    "isolatedModules": false
  },
  "include": ["src/**/*", "test-db.ts"],
  "exclude": ["node_modules"]
}